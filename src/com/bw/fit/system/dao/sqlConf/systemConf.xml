<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="systemSql">

	<!-- 菜单id查询其url -->
	<select id="getFrameUrlByMenuId" parameterType="String"
		resultType="menu">
		select * from menu a where fdid =#{menuId}
	</select>
	<select id="getDictByValue" parameterType="String"
		resultType="tdataDict">
		select * from data_dict a where a.isdeleted='0' and a.dict_value = #{dict_value}
	</select>
</mapper>   